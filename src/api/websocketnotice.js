/*  export function createwebsocketnotice(uid) {
    let websocket = null
    //判断当前浏览器是否支持WebSocket
  if("WebSocket" in window) {
    // console.log('建立连接')
    websocket = new WebSocket(
      "ws://localhost:8099/message/" + uid
    )
  }else {
    alert("不支持建立socket连接");
  }
  return websocket
} */
/*
ws: 协议指定了要创建的 WebSocket 连接的目标地址
 */
